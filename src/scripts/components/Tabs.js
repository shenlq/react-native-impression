import TabNavigator from 'react-native-tab-navigator';
import { textPrimary } from '../../styles/utils';

TabNavigator.Item.defaultProps.selectedTitleStyle = textPrimary;

export default TabNavigator;
