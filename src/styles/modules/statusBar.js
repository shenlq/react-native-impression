import { statusBar as style } from '../variables';

export const statusBar = {
    height: style.height,
    borderBottomWidth: style.borderBottomWidth,
    borderBottomColor: style.borderBottomColor,
    backgroundColor: style.backgroundColor,
};
